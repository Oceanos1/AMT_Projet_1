CREATE DATABASE IF NOT EXISTS amt_programs;
USE amt_programs;

CREATE TABLE IF NOT EXISTS program (
	id INT PRIMARY KEY AUTO_INCREMENT,
    language VARCHAR(30) DEFAULT NULL,
    type VARCHAR(30) DEFAULT NULL,
    version DOUBLE DEFAULT NULL
);